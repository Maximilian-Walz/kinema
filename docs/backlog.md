# Backlog

Open work now lives in **[GitHub Issues](https://github.com/Maximilian-Walz/kinema/issues)**,
not this file — issues are linkable, assignable, and referenceable from PRs
(`fixes #12`). Look for [`good first issue`](https://github.com/Maximilian-Walz/kinema/labels/good%20first%20issue)
if you're new.

Rough map of what's tracked there:

- **Bugs** — SCENE delete leaves default-visible elements (#2), no way to
  create/duplicate a real element (clip copy/paste mirrors the source) (#3),
  editing narration text leaves the recorded take stale (#4).
- **Repo / release** — CONTRIBUTING (#5), first release v0.1.0 (#6), unit tests
  for the server HTML patchers (#7), trust-model note + Dependabot (#8),
  windows CI (#9), demo media out of git history (#10).
- **Features** — music/SFX track with ducking (#11), loudness leveling at
  export (#12), recording ergonomics (#13), exit animation presets (#14),
  export options (#15), in-UI project management (#16), AI-workflow docs /
  MCP (#17).

Architecture orientation for the studio code lives in [../CLAUDE.md](../CLAUDE.md).
